import { ClosingTime } from "./ClosingTime";

/**
 * return
 * @targetNSAlias `tns`
 * @targetNamespace `http://wscbs/`
 */
export interface Return1 {
    /** closing_time[] */
    closing_time?: Array<ClosingTime>;
}
